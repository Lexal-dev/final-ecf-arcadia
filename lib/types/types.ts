export interface Animal {
    id: number;
    name: string;
    etat: string;
    imageUrl: string;
  }

export interface ImageData {
    name: string;
    url: string;
}

export  interface Service {
  id: number;
  name: string;
  description: string;
}
